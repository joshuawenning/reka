---
---

<div id="entries-section" class="entries-section">
  <div class="entries-header">
    <h2>Time Entries</h2>
    <button id="manual-entry-toggle" class="text-btn">+ Add manual entry</button>
  </div>
  
  <div id="manual-entry-form" class="manual-entry-form hidden">
    <div class="form-row">
      <input type="text" id="manual-project" placeholder="Project name" required />
    </div>
    <div class="form-row">
      <input type="text" id="manual-description" placeholder="Description (optional)" />
    </div>
    <div class="form-row form-row-split">
      <input type="date" id="manual-date" required />
      <input type="time" id="manual-start" required />
      <input type="time" id="manual-end" required />
    </div>
    <div class="form-actions">
      <button id="manual-cancel" class="btn-secondary">Cancel</button>
      <button id="manual-submit" class="btn-primary">Add Entry</button>
    </div>
  </div>
  
  <div id="entries-list" class="entries-list"></div>
  
  <div id="empty-state" class="empty-state hidden">
    <p>No time entries yet.</p>
    <p class="sub">Start tracking to see your work here.</p>
  </div>
</div>

<style>
  .entries-section {
    max-width: 720px;
    margin: 0 auto;
    padding: 0 24px 48px;
  }

  .entries-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
  }

  .entries-header h2 {
    font-size: 18px;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  .text-btn {
    color: var(--accent);
    font-weight: 500;
    padding: 8px 12px;
    border-radius: 6px;
    transition: background 0.15s ease-out;
  }

  .text-btn:hover {
    background: var(--surface);
  }

  .manual-entry-form {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 20px;
    margin-bottom: 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .manual-entry-form.hidden {
    display: none;
  }

  .form-row input {
    width: 100%;
  }

  .form-row-split {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 12px;
  }

  .form-actions {
    display: flex;
    gap: 12px;
    justify-content: flex-end;
    margin-top: 8px;
  }

  .btn-secondary {
    padding: 10px 20px;
    border-radius: 6px;
    font-weight: 500;
    color: var(--text-secondary);
    transition: all 0.15s ease-out;
  }

  .btn-secondary:hover {
    background: var(--bg);
    color: var(--text-primary);
  }

  .btn-primary {
    padding: 10px 20px;
    border-radius: 6px;
    font-weight: 500;
    background: var(--accent);
    color: white;
    transition: all 0.15s ease-out;
  }

  .btn-primary:hover {
    background: var(--accent-hover);
  }

  :global(.entries-list) {
    display: flex;
    flex-direction: column;
  }

  :global(.project-group) {
    margin-bottom: 24px;
  }

  :global(.project-header) {
    display: grid;
    grid-template-columns: 1fr 120px 100px 80px;
    gap: 16px;
    padding: 12px 16px;
    border-bottom: 1px solid var(--border);
    margin-bottom: 0;
  }

  :global(.project-name-header) {
    font-weight: 700;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-primary);
  }

  :global(.project-total) {
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--text-secondary);
    text-align: right;
  }

  :global(.project-total-label) {
    font-weight: 600;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
  }

  :global(.entry-row) {
    display: grid;
    grid-template-columns: 1fr 120px 100px 80px;
    gap: 16px;
    padding: 14px 16px;
    border-bottom: 1px solid var(--border);
    align-items: center;
    transition: background 0.1s ease-out;
  }

  :global(.entry-row:hover) {
    background: var(--surface);
  }

  :global(.entry-row:last-child) {
    border-bottom: none;
  }

  :global(.entry-info) {
    min-width: 0;
  }

  :global(.entry-description) {
    color: var(--text-primary);
    font-size: 14px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  :global(.entry-description.empty) {
    color: var(--text-muted);
    font-style: italic;
  }

  :global(.entry-meta) {
    font-size: 13px;
    color: var(--text-muted);
  }

  :global(.entry-duration) {
    font-family: var(--font-mono);
    font-weight: 500;
    font-size: 14px;
    color: var(--accent);
    text-align: right;
  }

  :global(.entry-actions) {
    display: flex;
    justify-content: flex-end;
    gap: 4px;
  }

  :global(.entry-action-btn) {
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    color: var(--text-muted);
    opacity: 0;
    transition: all 0.15s ease-out;
  }

  :global(.entry-row:hover .entry-action-btn) {
    opacity: 1;
  }

  :global(.entry-action-btn:hover) {
    background: var(--bg);
    color: var(--text-primary);
  }

  :global(.entry-action-btn.delete:hover) {
    color: var(--accent);
  }

  :global(.entry-edit-form) {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    padding: 16px;
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    align-items: start;
  }

  :global(.edit-field) {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  :global(.edit-field-label) {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
  }

  :global(.edit-input) {
    padding: 8px 10px;
    font-size: 13px;
    width: 100%;
  }

  :global(.edit-input-row) {
    display: flex;
    gap: 8px;
  }

  :global(.edit-input-row .edit-input) {
    flex: 1;
  }

  :global(.edit-actions) {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    align-items: flex-end;
  }

  :global(.edit-btn) {
    padding: 8px 16px;
    font-size: 13px;
    border-radius: 6px;
    font-weight: 500;
  }

  :global(.edit-btn.save) {
    background: var(--accent);
    color: white;
  }

  :global(.edit-btn.cancel) {
    color: var(--text-secondary);
  }

  .empty-state {
    text-align: center;
    padding: 48px 24px;
    color: var(--text-secondary);
  }

  .empty-state.hidden {
    display: none;
  }

  .empty-state .sub {
    font-size: 14px;
    color: var(--text-muted);
    margin-top: 8px;
  }

  @media (max-width: 640px) {
    :global(.project-header) {
      display: none;
    }

    :global(.entry-row) {
      grid-template-columns: 1fr auto;
      gap: 8px;
    }

    :global(.entry-meta), :global(.entry-duration) {
      display: none;
    }

    :global(.entry-row:hover .entry-action-btn) {
      opacity: 1;
    }

    .form-row-split {
      grid-template-columns: 1fr;
    }
  }
</style>
